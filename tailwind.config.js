module.exports = {
    content: ['./src/**/*.{html,js}'], // Điều chỉnh đường dẫn nếu cần
    theme: {
        extend: {
            fontFamily: {
                'playwrite': "'Playwrite IN', cursive;"
            },
        },
    },
    plugins: [],
};  